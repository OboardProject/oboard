# OBoard 界面规范：Calm Infrastructure

## 原则与边界

正确性、权限与可信结果优先于连续操作、响应速度、信息清晰、视觉与动画。正常时安静，操作立即回应，异常说明事实、影响和下一步。没有观测数据不等于正常，保存不等于远端生效。

本规范取代旧界面规范中“所有交互必须 spring/scale”“所有创建编辑必须 Dialog”“正常同步阶段必须常驻”的视觉要求。签名、资源授权、审批、事务、持久修订、版本门和迁移安全规则不变。沿用 React、现有组件、ModalSurface 和实时数据机制，不建设第二套弹窗、通知或部署系统。

单台服务器总览保留指标、基础信息、主要操作入口及相对位置。系统总览不是单台服务器总览。终端、图表和拓扑的手势、定位与专属布局不受普通按钮规则的强制变换影响。官网不在本规范迁移范围内。

## 视觉基础

唯一共享 Token 来源为 `web/src/style.css` 的浅色和深色变量，不在页面中另建颜色体系。

| 用途 | Token / 基准 |
| --- | --- |
| 页面、对象、浮层 | `--bg-page`、`--surface-solid`、`--surface-2` |
| 主、次、辅助文字 | `--text-primary`、`--text-secondary`、`--text-muted` |
| 分隔与可交互边界 | `--border`、`--border-strong`、`--control-border` |
| 主操作与选择 | `--primary`、`--primary-contrast`、`--primary-soft` |
| 键盘焦点 | `--focus-ring`，2px 轮廓，不只依赖阴影 |
| 成功、警告、错误 | `--color-success/warning/danger` 及对应 `-bg` |
| 处理中、未知 | `--color-processing/unknown` 及对应 `-bg` |
| 危险操作实底 | `--danger-fill`、`--danger-contrast`，不复用深色状态文字色 |
| 正文、数据、标题 | `--text-body-size` 14px（手机 15px）、`--text-data-size` 13px、`--text-title-size` 24px |
| 行高与间距 | `--line-body` 1.6；`--space-1/2/3/4/6/8` 对应 4/8/12/16/24/32px |
| 圆角 | 控件 8px，菜单 10px，窗口 12px；胶囊仅用于状态、标签等适当位置 |
| 控件 | 桌面常规 40px、紧凑 36px；手机共享操作目标至少 44px |

使用本地系统字体栈，优先支持中文，不以加载远程字体作为页面可用条件。技术 ID、IP、端口、版本、命令可等宽；流量、计数和百分比优先用 `font-variant-numeric: tabular-nums`。不将所有正文变成等宽字体。

普通文字对比度至少 4.5:1，足够大的文字至少 3:1。焦点与操作边界至少 3:1。颜色不单独承担在线、离线、禁用、未知或风险结论。浅色与深色需要分别检查，包括状态徽章的混合背景。

避免嵌套装饰卡片、大面积模糊、发光和渐变。卡片必须表示对象或真实分组，不因鼠标经过整体浮起。可点击对象用明确边界、文字和操作提示表达交互性。

## 术语与数据

- **服务器**：承载服务的机器；**节点**：提供订阅连接的配置对象，不与服务器互换。
- **代理链路**：对象间的连接关系；**任务**：一次执行过程。
- **保存**：应持久化的期望数据已被可靠确认；**生效**：满足对应运行确认条件。
- **需要处理**：从可靠事实源派生的未解决异常，不是另一套健康真相。
- **域名解析记录**与**服务 DNS 策略**是不同概念，必须明确命名。

`0` 表示测得为零，`—` 表示没有数据，`未采集` 表示采集未启用，`不限量` 表示无额度上限，`未知` 表示事实不足。不要将缺失数据填零。长名称允许换行或展开，IPv6 和命令允许局部横向阅读、复制；不能强迫整个页面横向滚动。

## 容器、焦点与移动端

根据任务选择容器，而非按比例替换组件：

- 小范围输入使用小弹窗或有边界的行内编辑。
- 当前对象编辑可使用侧栏；手机使用全屏编辑。
- 多步骤、多对象操作使用独立工作区。
- 删除、重启等短决策保持紧凑确认，不强制全屏。
- 查看信息的非模态面板不得声明 `aria-modal`。

`Dialog` 与 `MotionDialogPanel` 统一使用 `ModalSurface`。它负责唯一的层级、背景 inert、焦点栈、滚动锁、可视视口和退出交互控制；不能在页面再次叠加这些机制。`surfaceMotion="compact"`（包括 `Dialog size="sm"` 与既有紧凑确认类）保持紧凑，`form` / `workspace` 的普通手机窗口使用全屏可用空间；终端和危险确认保留专属布局。`none` 禁用尺寸过渡，不代表免除焦点和模态规则。

- 仅顶层接收 Escape、Tab 和背景点击；输入法组合过程中 Escape 不关闭编辑。
- 退出层不可点击、不可聚焦，不再声称自己是活动模态。
- 关闭恢复到合理的触发位置；只在确有未保存草稿时由编辑器处理放弃确认。
- 触屏默认聚焦面板，避免无意弹出软键盘。只有确有理由才显式指定 `autoFocus`。
- 可视视口缩小时保留可达的内容与操作；优先单一滚动容器，极短视口允许整个编辑窗口滚动。
- 保留浏览器缩放及双指缩放，不使用 `user-scalable=no`、`maximum-scale=1` 或全站手势拦截。
- 手机共享控件的点击区至少 44px，不要求图标本身同样大。导航使用抽屉，桌面保持稳定侧栏；不能以 hover 为唯一操作入口。

## 保存、反馈与恢复

保存策略由业务操作决定，不由控件动画决定：本地偏好可立即反映；低风险改名可受控乐观更新；路由、套餐、授权与代理链路使用整体校验和原子保存；删除、任务触发与凭据轮换使用明确命令，不自动重试。

提交快照和继续编辑的草稿是两个状态。返回只能确认对应快照；自动关闭必须同时匹配对象、编辑会话且没有后续修改。未知结果不是明确失败，取消等待不等于取消服务端执行。没有对应操作标识或权威证据时，不能因数据相似便宣称非幂等命令已成功。

可靠确认后局部更新权威实体，不串行等待所有列表和远端执行再释放表单。不相关操作保持可用。成功反馈优先采用实体更新、编辑器关闭或简短“已保存”，不重复弹保存、更新、同步成功。辅助技术通知不抢焦点、不逐阶段播报。

字段错误留在字段旁；明确拒绝或未知结果留在当前编辑器并保留输入；后台、跨页面问题进入持续问题区域。正常后台过程不常驻列出内部修订、哈希、协调阶段和写锁。异常先说事实、影响和下一步，技术细节仅按需展开。已读、重试中和隐藏都不是已解决；旧任务失败不能覆盖新版本生效事实。

## 动效与刷新

共享控件不使用默认缩放、弹跳或位移。`--ease-standard` 为无回弹的 ease-out：控件 100ms，菜单 140ms，弹窗 180ms，面板 220ms。短反馈可中断，不设人为最短等待时间，不靠动画完成事件决定业务成功。

页面用短淡入，不逐行错峰；MotionCard 默认不浮起；Toast 不弹跳；正常状态点不呼吸。只在明确进行中的工作保留加载提示，不让配置关系动画暗示真实流量。`prefers-reduced-motion` 覆盖共享 Motion 组件、CSS 元素及伪元素；去掉非必要位移与持续动画，仍保留明确的状态和文本。

首次加载才展示占位。后台刷新保留内容、输入、选择和位置；刷新失败保留旧数据并明确其陈旧状态。不要每个页面增加永久轮询，不整页重载代替一致性处理，不把敏感订阅、私钥、密码或令牌写入长期通用缓存。

## 验证入口

共享回归：`npm test -- src/components/ui/dialog.test.tsx src/components/ui/design-contract.test.ts src/components/ui/surface-motion.test.tsx src/components/ui/modal-viewport.test.ts src/components/ui/toast.test.tsx`。

其中 Token 数值检查不能替代浏览器检查。集成验收需要在实际运行界面检查浅/深色、360/390px 手机、768px 平板、1024px 与宽桌面、200% 缩放、键盘循环与恢复、退出层、reduced-motion、软键盘和 safe-area。终端与拓扑的专属交互也需独立验证。构建通过不证明所有场景通过，模拟数据不证明真实 Controller/Agent 联调或性能目标达标。
